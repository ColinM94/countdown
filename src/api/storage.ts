/* import { storage } from "./firebase"
 */
/* export function uploadImage() {
    const ref = storage.ref().child(`images/image.jpg`)

    ref.put(file).then((snapshot) => {
        alert("Success")
    }).catch(error => {
        alert(error.message)
    })
} */